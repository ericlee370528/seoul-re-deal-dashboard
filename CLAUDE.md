# CLAUDE.md

이 폴더에서 작업할 때 다음 규칙을 따른다.

## 규칙

1. **표는 마크다운으로 작성한다.** 텍스트로 나열하지 않고 마크다운 표 문법(`|`, `---`)을 사용한다.
2. **금액은 억원 단위로 표기한다.** 원 단위 숫자를 그대로 쓰지 않고 억원으로 환산하여 표기한다. (예: 1,250,000,000원 → 12.5억원)
3. **결론을 먼저 제시한다.** 분석·보고 결과를 작성할 때 배경 설명이나 과정보다 결론(요약)을 최상단에 배치한다.
4. **수치에는 계산 근거를 표시한다.** 산출된 숫자 옆에 계산식이나 출처를 함께 표기한다. (예: 12.5억원 = 매출 50억원 × 마진율 25%)

## 기술 규칙

이 폴더에서 웹 애플리케이션(대시보드 등)을 구현할 때는 다음을 따른다.

5. **순수 HTML/CSS/JS로 구현한다.** React·Vue 등 프레임워크나 별도 빌드 도구(webpack, vite 등)를 사용하지 않는다.
6. **파일은 `index.html`, `style.css`, `app.js` 3개로 유지한다.** 마크업/스타일/로직을 이 3개 파일로 분리하고, 이 이상으로 파일을 늘리지 않는다.
7. **데이터는 `localStorage`에 저장한다.** 앱 데이터를 브라우저의 `localStorage`에 저장·조회하여 새로고침 후에도 유지되도록 한다.

## 작업 규칙

8. **한 번에 한 태스크만 진행한다.** 여러 기능을 동시에 건드리지 않고, 하나를 끝낸 뒤 다음으로 넘어간다.
9. **변경 후 반드시 브라우저에서 확인한다.** 코드를 수정하면 실제로 브라우저에서 열어(또는 스크린샷으로) 동작을 확인한 뒤에 완료로 간주한다.
